#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(a==b)
    {
        printf("sum is:%d",(a+b)*2);
    }
          else
      {
        printf("sum is:%d",(a+b));
    }
    
}
